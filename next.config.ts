import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable the Next.js dev indicators (including the DevTools indicator)
  devIndicators: false,
};

export default nextConfig;
